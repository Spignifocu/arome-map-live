requests>=2.31
python-dotenv>=1.0
cfgrib>=0.9.15
xarray>=2024.1
eccodes>=1.7
# scipy>=1.12 exige numpy>=1.26, ce qui casserait le pin numpy ci-dessous
# (voir commentaire) -> on reste sur une version de scipy compatible.
scipy>=1.11,<1.12
# Épinglé <1.25 : les wheels numpy plus récentes exigent un baseline CPU
# (SSE4.2/AVX, "X86_V2") que certains hyperviseurs de VPS n'exposent pas
# au système invité, ce qui plante l'import avec un RuntimeError.
numpy>=1.23,<1.25
flask>=3.0
